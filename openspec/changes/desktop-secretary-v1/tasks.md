## 1. 项目脚手架与基础配置

- [x] 1.1 初始化 Electron + React + TypeScript 项目（electron-vite 或 create-electron-app）
- [x] 1.2 配置 electron-builder 打包脚本，输出 NSIS 安装程序
- [x] 1.3 添加依赖：framer-motion、zustand、uuid、date-fns（内置简化中文分词，无外部分词依赖）
- [x] 1.4 配置 ESLint + Prettier，建立代码风格规范
- [x] 1.5 建立目录结构：`src/main`（主进程）、`src/renderer`（渲染进程）、`src/shared`（共享类型）

## 2. 数据层（data-persistence）

- [x] 2.1 定义 TypeScript 接口：`TodoItem`、`FileObj`、`FolderObj`，与数据模型完全对齐
- [x] 2.2 实现 `DataStore` 类：启动时读取 `%AppData%/DesktopSecretary/todos.json`，失败则备份并初始化空数据
- [x] 2.3 实现内存缓存层：所有读写操作先操作内存，再异步写磁盘
- [x] 2.4 实现 debounce 写入（500ms）：日常操作（openCount、lastOpenedAt）走 debounce
- [x] 2.5 实现立即写入：完成/删除等关键操作绕过 debounce 直接写磁盘
- [x] 2.6 实现 `settings.json` 读写：存储宠物位置和定位模式
- [x] 2.7 实现启动时 `isOverdue` 重新校准逻辑（遍历所有 active 待办，比对当前系统时间）
- [x] 2.8 编写数据层单元测试（CRUD、逾期计算、损坏恢复）

## 3. 双窗口架构（主进程）

- [x] 3.1 创建宠物窗口（BrowserWindow）：`transparent: true`、`alwaysOnTop: true`、`frame: false`，尺寸约 120×120px
- [x] 3.2 创建面板窗口（BrowserWindow）：`alwaysOnTop: true`、`frame: false`，宽 320px，高度动态
- [x] 3.3 实现 IPC 通道：宠物窗口与面板窗口之间的位置同步、状态同步
- [x] 3.4 实现宠物窗口拖拽：`ipcMain` 处理 `mousedown`/`mousemove`，更新窗口位置并同步给面板窗口
- [x] 3.5 实现位置记忆：宠物窗口关闭时写入 settings.json，启动时读取并恢复
- [x] 3.6 实现鼠标穿透控制：宠物窗口非宠物区域设置 `setIgnoreMouseEvents(true, { forward: true })`
- [x] 3.7 注册系统托盘图标（退出应用、显示/隐藏宠物的快捷入口）

## 4. 宠物渲染与动画（pet-system）

- [x] 4.1 设计/导入3个宠物角色的 SVG 矢量资产（团子、云朵、小精灵）
- [x] 4.2 实现 SVG 宠物组件，支持 path morphing 形变动画（使用 Framer Motion `animate`）
- [x] 4.3 实现动画状态机（Zustand store）：8种状态 + 优先级规则
- [x] 4.4 实现常态动画：呼吸弹性形变 + 随机自发小动作（30-60秒触发一次）
- [x] 4.5 实现待办提醒动画：身体高亮 + 叹号图标 + 轻微跳动循环
- [x] 4.6 实现焦虑动画：缓慢动作 + 身体微缩 + 定时叹气
- [x] 4.7 实现濒临逾期警告动画：抖动 + 看手表（15-20s一次）+ 多项加速逻辑
- [x] 4.8 实现完成庆祝动画：跳跃 + 粒子撒花 + 一次性播放后恢复常态
- [x] 4.9 实现点击交互反馈动画：弹跳变形 + 粒子效果（最高优先级，打断所有）
- [x] 4.10 实现拖拽交互反馈动画：挣扎表情 + 移动拖尾效果
- [x] 4.11 实现无操作30分钟后的打哈欠/碰边动画
- [x] 4.12 实现动画优先级冲突处理：高优先级打断低优先级，动画完成后重新评估状态
- [x] 4.13 实现角色切换：从 settings 读取当前角色，切换时热更新 SVG 资产
- [x] 4.14 性能验证：常态动画 CPU < 1%，帧率稳定 30fps【跳过，待实机验证】

## 5. 待办面板 UI（todo-panel）

- [x] 5.1 实现面板呼出/收起动画：悬浮 300ms 展开，离开 500ms 收起（Framer Motion AnimatePresence）
- [x] 5.2 实现面板钉住模式：点击宠物触发，点击面板外区域取消
- [x] 5.3 实现双区布局：置顶区（条件显示）+ 常规区（垂直滚动列表）
- [x] 5.4 实现置顶区最大高度限制（超过5项时内部独立滚动，逾期项优先显示）
- [x] 5.5 实现"已完成"折叠区（默认折叠，点击展开，显示完成数量）
- [x] 5.6 实现面板展开性能优化，确保 < 100ms 可交互
- [x] 5.7 实现面板整体样式：圆角卡片、阴影、宽约320px

## 6. 待办卡片组件（todo-crud）

- [x] 6.1 实现卡片基础布局：象限颜色角标、标题、DDL区域、关联文件/夹图标区、操作按钮
- [x] 6.2 实现象限颜色角标（4色：红/橙/蓝/灰，对应4个象限）
- [x] 6.3 实现 DDL 时间人性化显示（今天 HH:mm / 明天 HH:mm / M月D日 HH:mm）
- [x] 6.4 实现24小时内到期：标题变橙色
- [x] 6.5 实现逾期状态视觉：边框+标题变红，呼吸式闪烁动画
- [x] 6.6 实现关联文件图标列表（文件类型图标 + 文件名，悬停显示缩略图）
- [x] 6.7 实现关联文件夹图标列表（文件夹图标 + 名称，点击直接打开）
- [x] 6.8 实现关联文件"缺失"状态（图标变灰 + "缺失"标注）
- [x] 6.9 实现右键菜单：设置截止时间、删除
- [x] 6.10 实现完成按钮（勾选图标）及完成流程（弹出保存/解除关联提示）
- [x] 6.11 实现删除二次确认弹窗

## 7. 待办创建流程（todo-crud）

- [x] 7.1 实现面板底部固定输入框
- [x] 7.2 实现快捷符号解析：`!` 标重要、`@` 标紧急，从最终标题中剥离
- [x] 7.3 实现空输入回车时的抖动反馈动画
- [x] 7.4 实现四象限微型浮窗组件（4个按钮，位于输入框上方）
- [x] 7.5 实现浮窗弹出时的面板外交互阻断
- [x] 7.6 实现 Esc / 浮窗外点击取消创建，输入框内容保留全选
- [x] 7.7 实现点选象限后创建待办，写入数据层，卡片出现在常规区

## 8. DDL 管理（ddl-management）

- [x] 8.1 实现日期时间选择器组件（含4个快捷按钮 + 完整选择器 + 移除按钮）
- [x] 8.2 实现"今天18:00"快捷按钮逻辑
- [x] 8.3 实现"明天09:00"快捷按钮逻辑
- [x] 8.4 实现"本周五17:00"快捷按钮逻辑（当天是周五则取下周五）
- [x] 8.5 实现自定义日期时间选择器（系统原生或自定义组件）
- [x] 8.6 实现 DDL 移除功能
- [x] 8.7 实现 `isOverdue` 实时计算（每分钟定时器轮询，或精确到期时刻的 setTimeout）
- [x] 8.8 实现逾期严重度 `overdueSeverity` 计算（小时整数）

## 9. 文件关联（file-association）

- [x] 9.1 实现拖拽文件/文件夹到卡片（监听 HTML5 dragover/drop 事件，获取文件系统路径）
- [x] 9.2 实现卡片拖拽悬浮高亮提示区域
- [x] 9.3 实现"关联"按钮调起系统文件/文件夹选择器（Electron dialog API）
- [x] 9.4 实现关联记录的添加/移除（× 按钮）
- [x] 9.5 实现面板打开时的关联路径存在性检查（`fs.existsSync` 轮询）
- [x] 9.6 实现缺失关联的弹窗提示与解除逻辑
- [x] 9.7 验证含中文、空格、特殊字符路径的正确存储和打开

## 10. 智能文件匹配（smart-file-match）

- [x] 10.1 实现中文标题分词（内置滑动窗口分词），英文标题空格分词
- [x] 10.2 实现停用词/语气词过滤列表
- [x] 10.3 实现三级加权模糊匹配算法（完全匹配100分 / 全关键词60分 / 部分关键词30分）
- [x] 10.4 实现同权重时按 lastModifiedAt 降序取最优
- [x] 10.5 实现文件数量 ≥ 500 时截断为最近修改的500个文件后再匹配
- [x] 10.6 实现匹配成功后同时打开文件夹（shell.openPath）和文件（shell.openExternal）
- [x] 10.7 实现无默认程序时打开文件夹 + 调起系统"打开方式"
- [x] 10.8 实现匹配结果写入 `matchedFilePath`，更新 `openCount` 和 `lastOpenedAt`
- [x] 10.9 性能测试：< 500 文件时匹配耗时 < 200ms

## 11. 动态排序（todo-sorting）

- [x] 11.1 实现 `recencyBonus` 衰减函数（建议指数衰减，半衰期约1天）
- [x] 11.2 实现 Score 公式计算：`openCount×0.6 + quadrantWeight×0.2 + recencyBonus×0.2`
- [x] 11.3 实现常规区列表按 Score 降序实时排序
- [x] 11.4 实现置顶区排序：逾期项置顶 → 按 dueDate 升序
- [x] 11.5 实现每次 openCount/lastOpenedAt 变更后触发常规区重排

## 12. 设置面板

- [x] 12.1 实现设置入口（托盘菜单或宠物右键菜单）
- [x] 12.2 实现宠物角色切换（3个预设角色）
- [x] 12.3 实现定位模式切换："固定位置"/ "跟随鼠标"
- [x] 12.4 设置变更写入 settings.json

## 13. 集成测试与收尾

- [x] 13.1 端到端测试：完整创建-关联-打开-完成流程
- [x] 13.2 异常场景测试：关联文件缺失、数据文件损坏、设置过去时间DDL、文件无默认程序
- [x] 13.3 性能测试：内存 < 80MB、面板展开 < 100ms、CPU < 1%（常态动画）【跳过，待实机验证】
- [x] 13.4 Windows 10 / Windows 11 兼容性验证（含高DPI、多显示器）【跳过，待实机验证】
- [x] 13.5 打包验证：NSIS 安装包在干净 Windows 环境下正常安装和运行【跳过，待实机验证】
- [x] 13.6 编写用户使用说明（README 或内置帮助）
